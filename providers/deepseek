#!/usr/bin/env bash
set -euo pipefail
export ANTHROPIC_AUTH_TOKEN="${CLAUDE_KEY_DEEPSEEK:?set CLAUDE_KEY_DEEPSEEK in ~/.bashrc}"
REPO="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
exec claude --settings "$REPO/providers/deepseek.json" "$@"
